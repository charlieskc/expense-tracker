# expense-tracker

- Legacy: `amex.py` Amex card scraper (existing).
- **Pantry Expense Dashboard (MVP):** see [`dashboard/`](./dashboard/) — Expo read-only spend UI against Neon `grok_pantry`.

```bash
cd dashboard && cp .env.example .env && npm install
npm run server   # terminal 1
npm start        # terminal 2
```

See `dashboard/VERIFICATION.md` for review steps.
